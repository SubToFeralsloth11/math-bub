/**
 * Legacy entry point replaced by `src/client.tsx` for TanStack Start.
 * This file is retained for migration reference - all entry logic has
 * moved to `client.tsx` (client-side) and `src/server.ts` (server entry).
 *
 * @module main
 * @author John Grimes
 */

// Entry logic moved to client.tsx.
console.debug("StudyBub: legacy main.tsx is intentionally inactive.");
